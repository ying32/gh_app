part of '../repo.dart';

class RepoActionPage extends StatelessWidget {
  const RepoActionPage(this.repo, {super.key});

  final QLRepository repo;

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('还没做'),
    );
  }
}
